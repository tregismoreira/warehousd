node_modules/
dist/
.next/
coverage/
.vitest-reports/
pnpm-lock.yaml
apps/web/next-env.d.ts
apps/web/test-results/
apps/web/playwright-report/

# Prose is out of scope. Markdown paragraphs are one line each and soft-wrap in the editor, so
# there is nothing for a formatter to reflow — and Prettier's proseWrap would fight that by
# re-imposing a print width. Prettier here is for code. See AGENTS.md, "Code conventions".
*.md

# Fixtures are records of what an external system produced, not source we write. Reformatting one
# makes a later diff against a fresh export unreadable.
test/keycloak/warehousd-realm.json
**/test/fixtures/

# The seed *content* (markdown documents, case files) is fixture data the example indexes.
# `live.ts` next to it is real source and is formatted.
examples/harbor/seed/*/
