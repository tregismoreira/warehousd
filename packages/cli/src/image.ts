// The GHCR namespace follows the GitHub repo owner. If the repo ever moves to an org,
// change this one line — older published CLI versions keep their baked-in ref.
export const IMAGE_REPO = "ghcr.io/tregismoreira/warehousd";
